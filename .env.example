# Path to pig.toml (default: ./pig.toml)
# PIG_CONFIG=/path/to/pig.toml

# Logging filter (default: warn)
# RUST_LOG=info

# Worker auth tokens — names are whatever you set in pig.toml auth_token_env
# MY_WORKER_TOKEN=<token>
