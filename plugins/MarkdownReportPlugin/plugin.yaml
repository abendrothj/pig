name: MarkdownReportPlugin
version: 0.1.0
description: Formats text into a Markdown report, optionally writing it to disk
maintainer: LAO Contributors
tags: [markdown, report, format, utility]
input:
  type: any
output:
  type: text
example_prompts:
  - "Turn this summary into a Markdown report"
  - "Write the analysis to report.md as Markdown"
