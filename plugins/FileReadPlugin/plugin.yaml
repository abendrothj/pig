name: FileReadPlugin
version: 0.1.0
description: Reads a file from disk and returns its contents as text
maintainer: LAO Contributors
tags: [file, io, read, utility]
input:
  type: any
output:
  type: text
example_prompts:
  - "Read the file at ./notes.txt"
  - "Load this document so the next step can summarize it"
