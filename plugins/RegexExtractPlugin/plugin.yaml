name: RegexExtractPlugin
version: 0.1.0
description: Extracts regex matches from text (first line is the pattern)
maintainer: LAO Contributors
tags: [regex, extract, text, utility]
input:
  type: any
output:
  type: text
example_prompts:
  - "Extract all email addresses from this text"
  - "Pull every dollar amount out of the document"
