[package]
name = "regex_extract_plugin"
version = "0.1.20"
edition = "2021"
description = "Extracts regex matches from text"
authors = ["Jake Abendroth <contact@jakea.net>"]

[dependencies]
lao_plugin_api = { path = "../../lao_plugin_api" }
regex = "1"

[lib]
crate-type = ["cdylib"]
