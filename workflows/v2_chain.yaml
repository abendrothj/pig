workflow: "V2 Chain"
schema_version: 2
steps:
  - id: locate_execution
    run: EchoPlugin
    input: "First step"
  - id: summarize
    run: EchoPlugin
    input_from:
      step: locate_execution
      output: output
    depends_on: [locate_execution]
